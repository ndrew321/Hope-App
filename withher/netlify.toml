[build]
  base = "withher/app"
  command = "npm install --legacy-peer-deps && npm run web:export"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
